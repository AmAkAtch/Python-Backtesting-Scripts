numpy>=1.24
pandas>=2.0
requests>=2.28
optuna>=3.4
# optional, used automatically if present:
#   ccxt      -> universal exchange fallback when Binance/Bybit/OKX are blocked
#   scipy     -> not required; indicators are pure numpy/pandas
